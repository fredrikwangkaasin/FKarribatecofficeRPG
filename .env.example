# GitHub Packages Authentication
# Required for building the Docker image (Nexus.MasterApi.Client NuGet package)
GITHUB_USERNAME=your-github-username
GITHUB_TOKEN=ghp_your_personal_access_token_here

# How to get a Personal Access Token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name (e.g., "Docker Desktop Shared")
# 4. Select scope: read:packages
# 5. Click "Generate token"
# 6. Copy the token (starts with ghp_) and replace the value above

# Note: Never commit your actual .env file with real credentials!
# This .env.example file is for reference only.
